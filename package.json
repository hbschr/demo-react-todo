{
  "name": "demo-react-todo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html --port 3000 --out-dir build/ --no-cache",
    "build": "parcel build src/index.html --public-url . --out-dir build/ --no-cache",
    "test": "echo 'Error: tests not configured for parcel' && exit 1"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "dependencies": {
    "normalize.css": "^8.0.1",
    "parcel-bundler": "^1.10.3",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-icons": "^3.2.2",
    "sass": "^1.15.1"
  }
}
